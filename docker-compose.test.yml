# docker-compose.yml
version: "3.9"

services:
  postgres:
    image: postgres:18.1-alpine    # Latest stable: 18.1 (Nov 13, 2025)
    restart: unless-stopped
    environment:
      POSTGRES_DB: nebula
      POSTGRES_USER: nebula
      POSTGRES_PASSWORD: nebula123  # Change in prod!
    ports:
      - "55432:5432"                 # Expose for local tools like pgAdmin
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nebula -d nebula"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:8.0-alpine        # Latest stable: 8.0 (Nov 4, 2025)
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning  # Basic persistence config
    ports:
      - "36379:6379"                # Expose for redis-cli or clients
    tmpfs:
      - /data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  rabbitmq:
    image: rabbitmq:4.1.5-management-alpine  # Latest stable: 4.1.5 (Oct 28, 2025) with management UI
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: nebula
      RABBITMQ_DEFAULT_PASS: nebula123  # Change in prod!
      RABBITMQ_DEFAULT_VHOST: nebula
    ports:
      - "5674:5672"                 # AMQP protocol
      - "15674:15672"               # Management UI (http://localhost:15672)
    tmpfs:
      - /var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  prometheus:
    image: prom/prometheus:latest
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.test.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=7d"
      - "--web.enable-lifecycle"
    extra_hosts:
      - "host.docker.internal:host-gateway"

  grafana:
    image: grafana/grafana:latest
    restart: unless-stopped
    ports:
      - "3050:3000"  # Grafana UI → http://localhost:3000
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin   # change in real env
      GF_SERVER_DOMAIN: localhost
      GF_SERVER_ROOT_URL: http://localhost:3000/
    tmpfs:
      - /var/lib/grafana
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    depends_on:
      - prometheus


volumes: {}
